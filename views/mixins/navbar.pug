mixin navbar(brandName, navItems, activePath)
  nav.header-nav.nav
    a.header-logo(href='/')
      img(src='/favicon/camel-logo.jpg', alt='Logo')
      span= brandName
    ul.nav-list
      each item in navItems
        li
          a.nav-link(
            href=item.url
            class={ active: activePath === item.url }
          )= item.label
